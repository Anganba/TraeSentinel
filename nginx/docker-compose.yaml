services:
  nginx:
    image: nginx:latest
    container_name: nginx
    restart: unless-stopped
    networks:
      - frontend
    volumes:
      - ./deploy:/usr/share/nginx/html:ro  # Mount your website files


    labels:
      - "traefik.enable=true"

      # ===============================================================
      # 🌍 HTTP Router (Traefik auto-redirects HTTP → HTTPS globally)
      # ===============================================================
      - "traefik.http.routers.nginx.rule=Host(`sigma.${DOMAIN}`)"
      - "traefik.http.routers.nginx.entrypoints=web"

      # ===============================================================
      # 🔒 HTTPS Secure Router
      # ===============================================================
      - "traefik.http.routers.nginx-secure.rule=Host(`sigma.${DOMAIN}`)"
      - "traefik.http.routers.nginx-secure.entrypoints=websecure"
      - "traefik.http.routers.nginx-secure.tls.certresolver=${PROVIDER}"
      - "traefik.http.routers.nginx-secure.service=nginx-svc"

      # 🧩 Apply global secure-headers middleware (already defined in Traefik)
      - "traefik.http.routers.nginx-secure.middlewares=secure-headers@docker"

      # ===============================================================
      # ⚙️ Service Definition
      # ===============================================================
      - "traefik.http.services.nginx-svc.loadbalancer.server.port=80"

networks:
  frontend:
    external: true
